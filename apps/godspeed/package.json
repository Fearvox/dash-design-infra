{
  "name": "@dash/godspeed",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "description": "godspeed coffee — site + light community layer. static HTML, Bun-built, Markdown-driven.",
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "dev": "bun --hot run src/build.ts --watch && bun serve dist/",
    "serve": "cd dist && bun serve",
    "typecheck": "tsc --noEmit",
    "test": "bun test src/"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "@types/marked": "^6.0.0"
  }
}
